# ⚡ СРОЧНО! Как исправить Netlify деплой

## 🎯 Проблема:
В build только 3 файла вместо полной сборки в dist

## ✅ Решение в 3 шага:

### ШАГ 1: Проверь Build Settings

Зайди на **Netlify Dashboard** → Твой сайт → **Site settings** → **Build & deploy**

Должно быть:
```
Build command: npm install && npm run build
Publish directory: dist
```

Если стоит `build` - **ИЗМЕНИ НА `dist`** и сохрани!

---

### ШАГ 2: Clear Cache & Redeploy

Зайди на **Deploys** → **Trigger deploy** → **Clear cache and deploy site**

Жди окончания сборки (1-3 минуты)

---

### ШАГ 3: Проверь результат

После деплоя открой **Deploy file browser** и проверь:

✅ ПРАВИЛЬНО:
```
├── index.html (300-500 B)
├── favicon.svg
├── yandex_9653b135babb0503.html
└── assets/
    ├── index-xxxxx.js (200KB+)
    └── index-xxxxx.css (50KB+)
```

❌ НЕПРАВИЛЬНО (как сейчас):
```
build/
├── index.html (380 B)
├── package.json
├── readme.md
└── vite.config.ts
```

---

## 🔥 Если не помогло:

### Вариант А: Смотри Build Log

1. Открой последний deploy
2. Кликни **Deploy log**
3. Найди ошибки (красный текст)
4. Скопируй ошибку и пришли мне

### Вариант Б: Деплой через CLI

```bash
# Установи Netlify CLI
npm install -g netlify-cli

# Залогинься
netlify login

# Собери локально
npm install
npm run build

# Задеплой вручную
netlify deploy --prod --dir=dist
```

### Вариант В: Создай новый сайт

1. В Netlify Dashboard создай **New site**
2. Подключи тот же GitHub repo
3. Настрой Build settings как в ШАГ 1
4. Деплой

---

## 📞 Чеклист перед деплоем:

- [ ] Build command: `npm install && npm run build`
- [ ] Publish directory: `dist` (НЕ build!)
- [ ] Node version: 18
- [ ] Git push сделан
- [ ] Cache очищен

---

## 🎉 После успешного деплоя:

Проверь что сайт работает:
- [ ] Главная страница загружается
- [ ] Нет белого экрана
- [ ] Стили применены
- [ ] Можно залогиниться
- [ ] `/yandex_9653b135babb0503.html` доступен

Готово! 🚀
