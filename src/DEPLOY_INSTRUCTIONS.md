# 🚀 ВАЖНО! Инструкция по деплою на Netlify

## ⚠️ ПРОБЛЕМА: Build папка вместо dist

Вижу что у тебя в деплое папка `build` с 3 файлами вместо `dist` с полной сборкой.

## ✅ Решение:

### Шаг 1: Очисти и задеплой заново

В Netlify Dashboard:

1. **Deploys** → **Trigger Deploy** → **Clear cache and deploy site**
2. Жди пока сборка завершится
3. Смотри логи сборки

### Шаг 2: Проверь Build Settings

В Netlify Dashboard → **Site settings** → **Build & deploy** → **Build settings**:

```
Build command: npm install && npm run build
Publish directory: dist
```

### Шаг 3: Environment variables

В **Site settings** → **Build & deploy** → **Environment**:

```
NODE_VERSION = 18
```

## 📋 Что исправлено:

1. ✅ Удалены папки `_headers` и `_redirects` (они становятся папками из-за Figma Make)
2. ✅ Все настройки перенесены в `netlify.toml`
3. ✅ Создан правильный `vite.config.ts` с `outDir: "dist"`
4. ✅ Упрощен `package.json`

## 🔍 Если сборка падает:

Смотри Build Log в Netlify и ищи:

1. **ОШИБКИ установки зависимостей:**
   ```
   npm ERR!
   ```
   → Проверь что версии в `package.json` корректные

2. **ОШИБКИ TypeScript:**
   ```
   TS2xxx: ...
   ```
   → У нас отключены строгие проверки в `tsconfig.json`

3. **ОШИБКИ импортов:**
   ```
   Cannot find module ...
   ```
   → Проверь что все компоненты существуют

## 🎯 Ожидаемый результат:

После сборки в папке `dist` должно быть:

```
dist/
├── index.html
├── assets/
│   ├── index-[hash].js
│   ├── index-[hash].css
│   └── ... (другие чанки)
├── favicon.svg
└── yandex_9653b135babb0503.html
```

## 🧪 Локальный тест:

```bash
# Установи зависимости
npm install

# Собери проект
npm run build

# Проверь что папка dist создалась
ls -la dist

# Запусти preview
npm run preview
```

Если локально все работает - значит проблема в Netlify настройках!

## 🆘 Если ничего не помогает:

1. **Удали** deploy полностью в Netlify
2. **Создай новый** site
3. **Подключи** GitHub repo заново
4. **Настрой** Build settings по инструкции выше
