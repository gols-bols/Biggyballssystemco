# 🐛 Debug: Почему в build только 3 файла?

## Проблема:
В деплое папка `build` содержит только 3 файла:
- index.html (380 B)
- package.json (2.2 KB)
- readme.md (570 B)
- vite.config.ts (2.8 KB)

А должна быть папка `dist` с полной сборкой!

## Причины:

### 1. ❌ Netlify собирает не ту папку

**Проверь Build Settings:**
```
Build command: npm install && npm run build  ✅
Publish directory: dist  ← ДОЛЖНО БЫТЬ ТАК!
```

Если там стоит `build` - измени на `dist`!

### 2. ❌ Сборка падает с ошибкой

Netlify может пропустить ошибку сборки и задеплоить исходники.

**Смотри Build Log:**
- Открой последний deploy
- Нажми "Deploy log"
- Ищи красные ошибки

Типичные ошибки:
```
npm ERR! code ERESOLVE
→ Проблема с зависимостями

TS2307: Cannot find module
→ Проблема с импортами

Error: PostCSS plugin @tailwindcss/postcss
→ Проблема с Tailwind
```

### 3. ❌ Node.js неправильная версия

**Проверь Environment:**
```
NODE_VERSION = 18
```

### 4. ✅ Как исправить:

**Вариант 1: Clear cache and redeploy**
```
Deploys → Trigger Deploy → Clear cache and deploy site
```

**Вариант 2: Через Netlify CLI**
```bash
# Установи Netlify CLI
npm install -g netlify-cli

# Залогинься
netlify login

# Деплой
netlify deploy --prod
```

**Вариант 3: Локальная проверка**
```bash
# Удали node_modules и dist
rm -rf node_modules dist

# Установи заново
npm install

# Собери
npm run build

# Проверь что dist создалась
ls -la dist/

# Должно быть примерно так:
# dist/
#   index.html
#   assets/
#     index-xxxxx.js
#     index-xxxxx.css
#   favicon.svg
#   yandex_9653b135babb0503.html
```

## 🎯 Ожидаемая структура dist:

После успешной сборки в `dist/` должно быть:

```
dist/
├── index.html                    # Главная страница
├── favicon.svg                   # Иконка
├── yandex_9653b135babb0503.html # Верификация
└── assets/                       # Собранные JS/CSS
    ├── index-[hash].js          # React приложение
    ├── index-[hash].css         # Стили
    └── [chunks]-[hash].js       # Другие чанки
```

Размер dist должен быть около **500KB - 2MB**.

## 🔍 Проверь Build Log:

Открой последний deploy в Netlify и ищи строки:

```
✅ ХОРОШО:
> vite build
✓ built in XXms
dist/index.html

✅ SUCCESS: Build completed

❌ ПЛОХО:
ERROR: Build failed
npm ERR!
```

Если видишь ошибки - скопируй их и исправь!
