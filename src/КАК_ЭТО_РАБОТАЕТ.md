# 🔥 КАК ЭТО РАБОТАЕТ

## ⚠️ ВАЖНО: Папка `dist` НЕ в Git!

**Папка `dist` создается автоматически при сборке!**

Она **НЕ ДОЛЖНА** быть в репозитории - поэтому в `.gitignore` добавлен `dist`.

## 🔄 Процесс деплоя на Netlify:

### 1. Git Push
```bash
git add .
git commit -m "Update"
git push
```

Netlify получает твой код БЕЗ папки dist.

---

### 2. Netlify запускает сборку

На сервере Netlify выполняется:

```bash
# 1. Клонирует репозиторий
git clone your-repo

# 2. Устанавливает зависимости
npm install

# 3. Запускает сборку
npm run build
# ↑ ЭТО создает папку dist!
```

---

### 3. Результат сборки

После `npm run build` создается папка `dist/`:

```
dist/
├── index.html
├── assets/
│   ├── index-abc123.js  ← React приложение
│   └── index-xyz456.css ← Стили
├── favicon.svg
└── yandex_9653b135babb0503.html
```

---

### 4. Netlify публикует dist

Netlify берет содержимое папки `dist` и публикует на домене.

---

## 🎯 Твоя проблема:

**В Netlify Deploy file browser видно:**
```
build/
├── index.html
├── package.json
├── readme.md
└── vite.config.ts
```

Это значит что:
1. ❌ Сборка **НЕ выполнилась** (`npm run build` не запустился или упал)
2. ❌ Netlify задеплоил **исходники** вместо собранного проекта
3. ❌ В Build Settings указана папка `build` вместо `dist`

---

## ✅ Решение:

### Шаг 1: Проверь настройки

Netlify Dashboard → Site settings → Build & deploy:

```
Build command: npm install && npm run build
Publish directory: dist  ← ПРОВЕРЬ ЭТО!
```

---

### Шаг 2: Проверь Build Log

Deploys → Последний deploy → Deploy log

Ищи:
```
✅ ХОРОШО:
$ npm run build
vite v5.0.8 building for production...
✓ 150 modules transformed.
dist/index.html                   0.45 kB
dist/assets/index-abc123.js     250.00 kB
dist/assets/index-xyz456.css     50.00 kB
✓ built in 2.5s

✅ Build succeeded!

❌ ПЛОХО:
npm ERR! code ERESOLVE
Build failed
ERROR: Command failed
```

---

### Шаг 3: Локальный тест

Проверь что сборка работает у тебя:

```bash
# Очисти всё
rm -rf node_modules dist

# Установи зависимости
npm install

# Собери проект
npm run build

# Проверь что dist создалась
ls -la dist/
# Должно быть:
# index.html
# assets/
# favicon.svg
# yandex_9653b135babb0503.html

# Запусти preview
npm run preview
# Открой http://localhost:4173
```

Если **локально** всё работает → проблема в Netlify настройках!

---

## 🚀 Полная инструкция:

### 1. Убедись что `.gitignore` содержит `dist`
✅ Уже сделано

### 2. Git push (БЕЗ папки dist!)
```bash
git add .
git commit -m "Fix build config"
git push
```

### 3. В Netlify измени настройки
```
Publish directory: dist
```

### 4. Clear cache and redeploy
Deploys → Trigger deploy → Clear cache and deploy site

### 5. Проверь Build Log
Должно быть "Build succeeded!"

### 6. Проверь Deploy file browser
Должна быть папка `assets/` с JS и CSS

---

## 📊 Схема работы:

```
Твой компьютер:
├── App.tsx              ← Исходный код
├── components/          ← Исходный код
├── package.json         ← Конфигурация
└── .gitignore           ← dist игнорируется!

↓ git push

GitHub:
├── App.tsx              ← Только исходники
├── components/
└── package.json

↓ Netlify clone & build

Netlify сервер:
├── App.tsx              ← Клонировал
├── components/
├── node_modules/        ← npm install
└── dist/                ← npm run build СОЗДАЛ!
    ├── index.html
    └── assets/

↓ Netlify publish

Netlify CDN:
└── dist/                ← Только это публикуется!
    ├── index.html
    └── assets/
```

---

## 🎯 Итог:

1. **НЕ создавай** папку dist вручную
2. **НЕ коммить** dist в git
3. **Netlify сам** создаст dist при сборке
4. **Проверь** что в Netlify указано `Publish directory: dist`

Готово! 🚀
