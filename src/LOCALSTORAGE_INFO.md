# 💾 Информация о хранилище данных (LocalStorage)

## Описание

Система управления заявками использует **LocalStorage** браузера для постоянного хранения данных в формате JSON. Это позволяет сохранять все изменения между сеансами работы без необходимости использования внешней базы данных.

## Что хранится в LocalStorage

### 1. `tickets_db` - База данных заявок
Хранит массив всех заявок в формате JSON со следующими полями:
- `id` - уникальный идентификатор
- `title` - заголовок заявки
- `description` - описание проблемы
- `category` - категория (Оборудование, Доступы, Программное обеспечение)
- `priority` - приоритет (Низкий, Средний, Высокий)
- `status` - статус (Новая, В работе, Завершена)
- `author` - автор заявки
- `assignedTo` - назначенный исполнитель
- `createdAt` - дата создания (ISO формат)
- `updatedAt` - дата обновления (ISO формат)
- `history` - история изменений заявки

### 2. `users_db` - База данных пользователей
Хранит массив зарегистрированных пользователей:
- `username` - логин пользователя
- `password` - пароль (в открытом виде, только для демонстрации)
- `displayName` - отображаемое имя
- `email` - электронная почта
- `role` - роль пользователя ('admin' или 'user')

**Начальные пользователи:**
- **Администратор:** логин `admin`, пароль `admin123`
- **Пользователь:** логин `user`, пароль `user123`

### 3. `current_user` - Текущий пользователь
Хранит информацию о текущем авторизованном пользователе для автоматического входа при повторном открытии приложения.

## Механизм работы

### Загрузка данных при старте
1. При инициализации приложения данные загружаются из LocalStorage
2. Если данные отсутствуют (первый запуск), используются начальные данные
3. Текущий пользователь автоматически восстанавливается (авто-вход)

### Сохранение изменений
Все изменения автоматически сохраняются в LocalStorage через React `useEffect`:
- Создание/редактирование/удаление заявок
- Регистрация новых пользователей
- Вход/выход пользователей
- Изменение статусов и назначение исполнителей

### Сброс данных
Для сброса всех данных к начальным значениям:
1. Нажмите кнопку **"Сбросить данные"** в футере страницы
2. Подтвердите действие в диалоговом окне
3. Все данные будут удалены, и страница перезагрузится

Также можно очистить LocalStorage вручную через консоль браузера:
```javascript
localStorage.clear();
location.reload();
```

## Преимущества LocalStorage

✅ **Простота** - не требует настройки сервера или базы данных  
✅ **Скорость** - мгновенное чтение и запись данных  
✅ **Постоянство** - данные сохраняются между сеансами  
✅ **Автономность** - работает полностью офлайн  

## Ограничения LocalStorage

⚠️ **Объем** - ограничение около 5-10 МБ на домен  
⚠️ **Браузер** - данные привязаны к конкретному браузеру  
⚠️ **Безопасность** - данные хранятся в открытом виде (не подходит для чувствительных данных)  
⚠️ **Синхронизация** - нет синхронизации между устройствами  
⚠️ **Очистка** - данные могут быть удалены при очистке кэша браузера  

## Просмотр данных

### Через DevTools браузера:
1. Откройте консоль разработчика (F12)
2. Перейдите на вкладку **Application** (Chrome) или **Storage** (Firefox)
3. Найдите раздел **Local Storage** → выберите ваш домен
4. Просмотрите ключи: `tickets_db`, `users_db`, `current_user`

### Через консоль JavaScript:
```javascript
// Просмотр всех заявок
console.log(JSON.parse(localStorage.getItem('tickets_db')));

// Просмотр всех пользователей
console.log(JSON.parse(localStorage.getItem('users_db')));

// Просмотр текущего пользователя
console.log(JSON.parse(localStorage.getItem('current_user')));
```

## Экспорт/Импорт данных

### Экспорт данных в JSON файл:
```javascript
// Экспорт всех данных
const data = {
  tickets: JSON.parse(localStorage.getItem('tickets_db')),
  users: JSON.parse(localStorage.getItem('users_db'))
};
const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' });
const url = URL.createObjectURL(blob);
const a = document.createElement('a');
a.href = url;
a.download = 'backup.json';
a.click();
```

### Импорт данных из JSON:
```javascript
// Вставьте ваши данные в переменную data
const data = { /* ваши данные */ };
localStorage.setItem('tickets_db', JSON.stringify(data.tickets));
localStorage.setItem('users_db', JSON.stringify(data.users));
location.reload();
```

## Рекомендации

1. **Регулярное резервное копирование** - экспортируйте данные для сохранности
2. **Тестирование** - используйте режим инкогнито для тестирования без влияния на основные данные
3. **Мониторинг объема** - следите за количеством заявок (в футере отображается статистика)
4. **Очистка старых данных** - периодически удаляйте завершенные заявки

## Миграция на Supabase

Для реального производственного использования рекомендуется мигрировать на полноценную базу данных Supabase:
- Реальная аутентификация пользователей
- Синхронизация данных между устройствами
- Безопасное хранение паролей
- Резервное копирование
- Масштабируемость

Свяжитесь с разработчиком для настройки интеграции с Supabase.

---

**Версия:** 1.0  
**Дата:** Ноябрь 2025  
**Формат данных:** JSON  
**Кодировка:** UTF-8
