# 🔧 Инструкция по деплою

## ✅ Что исправлено:

1. **Структура проекта:**
   - ✅ Создана папка `/src/` с `main.tsx`
   - ✅ Исправлен путь в `index.html` на `/src/main.tsx`
   - ✅ Удалены папки `_headers` и `_redirects`, созданы файлы
   - ✅ Добавлен favicon

2. **Конфигурация:**
   - ✅ Упрощен `vite.config.ts`
   - ✅ Ослаблены требования TypeScript
   - ✅ Настроен `postcss.config.js`
   - ✅ Обновлен `netlify.toml`

3. **Netlify настройки:**
   ```
   Build command: npm install && npm run build
   Publish directory: dist
   Node version: 18
   ```

## 🚀 Как задеплоить:

### Вариант 1: Git Push (рекомендуется)

```bash
git add .
git commit -m "Fix build config and structure"
git push
```

Netlify автоматически начнет деплой.

### Вариант 2: Manual Deploy

В Netlify Dashboard:
- Нажми **"Trigger deploy"** → **"Deploy site"**

## 🧪 Локальная проверка:

```bash
# Установка
npm install

# Запуск dev сервера
npm run dev

# Тест сборки
npm run build
npm run preview
```

## 📋 Checklist после деплоя:

- [ ] Главная страница загружается
- [ ] Нет белого экрана
- [ ] Консоль без ошибок MIME type
- [ ] Файл верификации доступен: `/yandex_9653b135babb0503.html`
- [ ] Favicon отображается
- [ ] Все стили применены

## 🐛 Если все еще белый экран:

1. Открой **DevTools** → **Console**
2. Проверь есть ли ошибки
3. Проверь **Network** → есть ли 404 ошибки
4. Проверь что `/src/main.tsx` загружается с MIME type `application/javascript`

## 📞 Netlify Build Log

Смотри логи сборки в Netlify Dashboard чтобы увидеть где именно происходит ошибка.
