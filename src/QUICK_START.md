# ⚡ QUICK START - Исправь Netlify за 2 минуты

## 🚨 ТВОЯ ПРОБЛЕМА:
В Netlify Deploy file browser видишь папку `build` с 3 файлами вместо правильной сборки.

## ℹ️ ВАЖНО:
Папка `dist` НЕ ДОЛЖНА быть в Git! Она создается автоматически при сборке на Netlify.

## ✅ РЕШЕНИЕ В 4 ШАГА:

### 1️⃣ Открой Netlify Build Settings
Netlify Dashboard → Твой сайт → **Site settings** → **Build & deploy** → **Build settings**

### 2️⃣ Проверь настройки
Должно быть **ТОЧНО ТАК**:
```
Build command: npm install && npm run build
Publish directory: dist
```

Если стоит `build` → **ИЗМЕНИ НА `dist`** и жми **Save**!

### 3️⃣ Clear Cache & Redeploy
**Deploys** → **Trigger deploy** → **Clear cache and deploy site**

### 4️⃣ Жди 2-3 минуты и проверяй!

---

## 🎯 Как проверить что всё ОК:

После деплоя открой **Deploy file browser** в Netlify:

✅ **ПРАВИЛЬНО** (должно быть):
```
├── index.html
├── favicon.svg
├── yandex_9653b135babb0503.html
└── assets/
    ├── index-abc123.js (200KB+)
    └── index-xyz456.css (50KB+)
```

❌ **НЕПРАВИЛЬНО** (сейчас):
```
build/
├── index.html (380 B)
├── package.json
└── vite.config.ts
```

---

## 🔥 Если всё равно не работает:

### Вариант 1: Проверь Build Log
Deploys → Последний deploy → **Deploy log**

Ищи ошибки (красный текст). Если видишь "Build succeeded!" → проблема в настройках.

### Вариант 2: Локальный тест
```bash
npm install
npm run build
ls -la dist/  # Должна создаться папка dist!
```

Если локально работает → проблема точно в Netlify настройках!

---

## 📚 Подробности:

Читай `КАК_ЭТО_РАБОТАЕТ.md` - там объяснено почему dist не в Git и как работает сборка.

---

## ✅ Checklist:

- [ ] В Netlify: Publish directory = `dist` (НЕ build!)
- [ ] Build command = `npm install && npm run build`
- [ ] Сделал Clear cache and deploy
- [ ] Проверил Deploy log - нет ошибок
- [ ] В Deploy file browser есть папка assets

Готово! 🚀