# ⚡ БЫСТРЫЙ ДЕПЛОЙ

## 🚀 ДЕЛАЙ:

### 1. Git Push
```bash
git add .
git commit -m "Fix build"
git push
```

### 2. В Netlify UI

**Site settings** → **Build & deploy** → **Build settings** → **Edit settings**

1. **Build command**: ОЧИСТИ (пусто!)
2. **Publish directory**: ОЧИСТИ (пусто!)
3. **Save**

### 3. Deploy

**Deploys** → **Trigger deploy** → **Clear cache and deploy site**

---

✅ Готово! Netlify сам найдёт папку с build!