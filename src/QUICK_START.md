# ⚡ QUICK START - Исправь Netlify за 2 минуты

## ✅ КОНФИГИ УЖЕ ИСПРАВЛЕНЫ!

Проблема была: Vite собирал в папку `build/` вместо `dist/`.

Исправлено:
- ✅ Обновлён `vite.config.ts`
- ✅ Обновлён `package.json` (Vite 6.x)
- ✅ Обновлён `netlify.toml`

---

## 🚀 ЧТО ДЕЛАТЬ СЕЙЧАС:

### 1️⃣ Git Push
```bash
git add .
git commit -m "Fix Vite build config"
git push
```

### 2️⃣ Очисти Build Settings в Netlify

**ВАЖНО!** Зайди в Netlify UI:

**Site settings** → **Build & deploy** → **Build settings** → **Edit settings**

1. **Build command**: **ОЧИСТИ** (оставь пустым!) - чтобы использовался netlify.toml
2. **Publish directory**: проверь что стоит `dist`
3. **Save**

### 3️⃣ Clear Cache & Deploy

**Deploys** → **Trigger deploy** → **Clear cache and deploy site**

### 4️⃣ Проверь логи!

Должно быть:
```
dist/index.html    ← DIST, не build!
dist/assets/...
✓ built in 2.42s
```

---

## 🎯 Если работает:

Сайт задеплоится через 2-3 минуты! 🎉

## 🆘 Если не работает:

Пришли новые логи или читай `AFTER_FIX.md` для детальной отладки.

---

Готово! 🚀