
  # Интерактивный макет системы заявок

  This is a code bundle for Интерактивный макет системы заявок. 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
